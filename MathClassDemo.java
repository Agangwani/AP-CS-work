import java.lang.Math;
import java.text.DecimalFormat;

public class MathClassDemo 
{

	public static void main (String[]args)
	{
		/*
		//given the area of a circle, compute its radius
		double area = 10.0;
		double radius;
		radius = Math.sqrt(area / Math.PI);
		System.out.println("the radius when the area is 10 is " + radius);
		
		DecimalFormat myFormat = new DecimalFormat("####.####");
		System.out.println(myFormat.format(radius));
		
		DecimalFormat myFormat2 = new DecimalFormat("00000");
		System.out.println(myFormat2.format(radius));
		
		DecimalFormat myFormat3 = new DecimalFormat("##,###.00");
		System.out.println(myFormat3.format(radius));
		*/
		
		//int m;
		double x;
		//m = Math.abs(-7);
		//x = Math.abs(-7.5);
		
		x = Math.pow(2.0,3.0);
		//x = Math.pow(16, 0.25);
		/*
		m = Math.max(20, 30);
		m = Math.min(20,30);
		
		m = (int) Math.round(3.94);
		m = (int) Math.floor(3.99);
		m = (int) Math.ceil(3.1111);
		*/
		
		
		//System.out.println(m);
		System.out.println(x);
		
	}
}
